---
description: Run all routine local upgrades in one flow — Homebrew, gstack, and Claude Code plugins. Runs the upgrade-all skill. Desktop-executable entry point.
argument-hint: "[optional — leave empty for the full upgrade run]"
---

Invoke the **upgrade-all** skill via the Skill tool and hand it the user's request verbatim as its arguments:

$ARGUMENTS

Do not reimplement the upgrade steps yourself. Hand off immediately and let the upgrade-all skill drive the entire run.
