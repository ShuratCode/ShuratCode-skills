---
description: Answer a question from the vault's own notes, with citations — never from general knowledge. Runs the vault-query skill. Desktop-executable entry point.
argument-hint: "[your question about what the vault says]"
---

Invoke the **vault-query** skill via the Skill tool and hand it the user's request verbatim as its arguments:

$ARGUMENTS

Do not answer from general knowledge. Hand off immediately and let the vault-query skill answer from the vault, deferring to the vault's AGENTS.md as its source of truth.
