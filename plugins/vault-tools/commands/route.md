---
description: Sweep 00 Inbox and route each captured note to its correct home — articles to Reading/Inbox (normalised), known types to their PARA folder, ambiguous notes left flagged. Runs the vault-route skill. Desktop-executable entry point.
argument-hint: "[optional — leave empty to sweep the whole 00 Inbox]"
---

Invoke the **vault-route** skill via the Skill tool and hand it the user's request verbatim as its arguments:

$ARGUMENTS

Do not move files behind Shaked's back. Hand off immediately and let the vault-route skill drive the sweep — propose the routing table first, move only after he confirms — deferring to the vault's AGENTS.md as its source of truth.
