---
description: Health-check Shaked's Notion second brain and produce a report of things to fix — never silent edits. Runs the brain-lint skill. Desktop-executable entry point.
argument-hint: "[optional focus, e.g. 'orphans', 'duplicates', 'pipeline drift']"
---

Invoke the **brain-lint** skill via the Skill tool and hand it the user's request verbatim as its arguments:

$ARGUMENTS

Do not edit anything yourself. Hand off immediately and let the brain-lint skill drive the run, running the workspace guard and deferring to the Notion Agent Handbook as its source of truth. Lint proposes; Shaked disposes.
